这是一个基于 **v5.0 (PuppetMaster & Director)** 技术文档生成的项目任务看板文件。你可以将其直接导入到 Jira, Trello, Notion 或 Github Projects 中使用。

---

# 📽️ 通用剧本杀自动化引擎 - 开发任务看板

**项目代号**: PuppetMaster & Director
**当前阶段**: 初始化 / 架构搭建

---

## 📌 Milestones (里程碑)
- [ ] **M1: 协议与骨架** - 完成 YAML 定义与 Python 基础加载器。
- [ ] **M2: 核心循环** - 能够跑通纯文本的剧本杀流程 (Concordia + Dify Player)。
- [ ] **M3: 视觉导演** - 接入 Director Agent，能够输出分镜 JSON。
- [ ] **M4: 完整闭环** - 剧本输入 -> 自动化演绎 -> 分镜脚本输出 -> ComfyUI 对接。

---

## 📋 To Do (待办事项)

### 🏗️ 基础设施 & 协议层 (Infrastructure & Protocol)
- [ ] **[P0] 设计 YAML Schema v2.0**
    - 定义 `meta`, `knowledge_config`, `characters` 结构。
    - **重点**: 定义 `visual_style` (base_prompt, char_map) 和 `phases` 的状态机结构。
- [ ] **[P0] 编写 ScriptLoader 类**
    - 实现 YAML 读取与校验逻辑。
    - 实现 `visual_style` 的解析与对象化。
- [ ] **[P1] 准备测试剧本数据**
    - 编写 `demo_case.yaml` (包含少量对话和场景)。
    - 准备对应的知识库文档 (TXT/MD)，并添加 `【私有标签】`。

### ⚙️ 仿真引擎层 (Simulation - Python/Concordia)
- [ ] **[P0] 封装 UniversalDifyClient**
    - 实现对 Dify API 的 POST 请求封装。
    - 支持两种模式调用：`query_player` (推理) 和 `query_director` (导演)。
- [ ] **[P1] 实现 GameMaster (GM) 类**
    - 实现状态机逻辑 (`SEQUENTIAL_SPEAK` -> `FREE_DISCUSSION` 跳转)。
    - 实现 `Environment` 的初始化。
- [ ] **[P1] 实现 UniversalAgent (Player) 类**
    - 继承 Concordia Entity。
    - 实现 `act()` 方法：获取 Observation -> 组装 Context -> 调用 Dify -> 输出 Action。
- [ ] **[P1] 集成 Concordia 关联记忆 (Associative Memory)**
    - 配置向量数据库 (如 Chroma 或 Concordia 内置)。
    - 实现记忆检索逻辑 (Re-ranking 策略)。

### 🧠 认知层 (Cognition - Dify)
- [ ] **[P0] 搭建 "Player Reasoner" Workflow**
    - 输入变量: `role_profile`, `task`, `game_context`, `memory_summary`.
    - 节点: 知识库检索 (带标签过滤) -> LLM 推理 -> 输出。
- [ ] **[P0] 搭建 "Director Studio" Workflow**
    - 输入变量: `dialogue_text`, `base_style`, `char_map`, `scene_desc`.
    - System Prompt 编写: 专注于将文本转译为 SD/ComfyUI 提示词。
    - 输出格式约束: 强制 JSON 结构 (`shots`, `sd_prompt`).

### 🎬 视觉导演系统 (Visual Director System)
- [ ] **[P1] 实现 DirectorAgent 类**
    - 继承 Concordia Entity，但设为不可见/不发言。
    - 实现 `observe()`: 监听 EventStream。
    - 实现缓冲逻辑: 每 N 句对话或场景切换触发一次生成。
- [ ] **[P2] 开发分镜脚本导出器**
    - 定义 `visual_script.json` 的标准格式。
    - 实现将 Dify Director 的返回结果写入 JSON 文件。

### 🔌 外部集成 (Integration)
- [ ] **[P2] ComfyUI 桥接脚本 (可选)**
    - 编写 Python 脚本读取 `visual_script.json`。
    - (验证性) 调用 ComfyUI API 生成一张测试图。

---

## 🚧 In Progress (进行中)

- *暂无 (项目启动阶段)*

---

## 🧪 Testing & Review (测试与审查)

- [ ] **单元测试: ScriptLoader**
    - 验证 YAML 缺少字段时是否报错。
    - 验证 `visual_style` 是否正确加载。
- [ ] **集成测试: 记忆检索**
    - 模拟长文本输入，验证 Concordia 是否能召回 10 轮之前的关键信息。

---

## ✅ Done (已完成)

- [x] **[P0] 技术架构设计 (v5.0)**
    - 确定 Concordia + Dify + YAML + ComfyUI 技术栈。
    - 完成架构图与数据流设计。

---

## 📝 数据结构参考 (Data Schema Reference)

### 角色外观映射 (YAML)
```yaml
character_appearance:
  char_a: "1girl, police uniform, short hair, serious face"
  char_b: "1man, beard, grey coat, nervous"
```

### 分镜脚本输出 (JSON)
```json
{
  "timestamp": "00:01:20",
  "scene": "living_room",
  "sd_positive": "Cinematic, 1girl, police uniform, short hair, talking, looking at viewer, vintage room background",
  "subtitle": "我是警察，请配合调查。"
}
```
